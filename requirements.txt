aiogram>=3.15
aiosqlite>=0.20
httpx>=0.27
python-dotenv>=1.0
# HTTP-сервер мини-приложения (aiogram уже тянет его транзитивно — фиксируем
# версию явно, так как импортируем напрямую)
aiohttp>=3.9
# База часовых поясов IANA: обязательна на Windows (у системы её нет),
# на Linux/macOS безвредна
tzdata>=2024.1

# ЛОКАЛЬНОЕ распознавание речи (фолбэк, тянет PyTorch ~2 ГБ).
# Не нужно, если задан ASR_API_BASE (облачная транскрипция, напр. Groq):
# pip install openai-whisper
# openai-whisper>=20240930
